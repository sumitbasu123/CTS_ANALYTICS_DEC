version: 2

models:
  - name: stg_customers
    config:
      schema: "{{env_var('DBT_STAGESCHEMA','STAGE_DBT')}}"
    
  - name: stg_orders
    config:
      schema: "{{env_var('DBT_STAGESCHEMA','STAGE_DBT')}}"
